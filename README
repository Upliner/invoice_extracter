Программа извлекает данные счетов на оплату покупателям

Для работы программа требует следующие пакеты:
tesseract-ocr tesseract-ocr-rus python-pil antiword python-pyparsing poppler-utils

Версия tesseract должна быть не ниже 3.03
Также необходимо скопировать файл rusnum.traineddata в директорию tessdata, которая
может находиться по адресу /usr/share/tesseract-ocr/tessdata либо /usr/share/tessdata

Запуск программы:
python invoice_extracter.py [параметры] файлы_счетов

Параметры:
  -h, --help      Выводит краткую помощь по параметрам на английском языке
  -v, --verbose   Включает дополнительные сообщения:
                     * О найденных в файле некорректных ИНН
                     * О попытке повторить OCR-распознавание с большим разрешением

  --strict        "Строгий режим" -- удалять реквизиты организации либо банка
                  плательщика при наличии расхождений с веб-базами

  -q файл         Задать файл с реквизитами организации-плательщика
                  Файл состоит из строк в формате "реквизит: значение"
                  Допустимые реквизиты: Наименование, ИНН, КПП, р/с, БИК, Корсчет,
                  Банк (наименование банка)

  Задать реквизиты плательщика можно также с помощью следующих параметров:
    --inn ИНН
    --kpp КПП
    --acc Расчётный счёт
    --bic БИК
    --coracc Корсчёт
